apiVersion: v1
kind: Service 

metadata:
  name: fintrust-frontend
  labels:
    app: fintrust-frontend
spec:
  selector:
    app: fintrust-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  
  type: ClusterIP

