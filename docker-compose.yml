services:
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: fintrust
      POSTGRES_USER: fintrustuser
      POSTGRES_PASSWORD: fintrustpassword
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: fintrust-backend:v1.0
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: fintrustuser
      DB_PASSWORD: fintrustpassword
      DB_NAME: fintrust
    ports:
      - "8082:8080"
    depends_on:
      - db
    volumes:
      - ./backend:/app
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: fintrust-frontend:v1.0
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db_data:
    
  
      