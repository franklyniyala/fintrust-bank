apiVersion: v1
kind: Secret

metadata:
  name: postgres-exporter-secret
  namespace: monitoring

type: Opaque

stringData:
  DATA_SOURCE_NAME: "postgresql://fintrustuser:fintrustpassword@db.default.svc.cluster.local:5432/fintrust?sslmode=disable"