apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: fintrust-deployer-binding
  namespace: default

subjects:

- kind: ServiceAccount

  name: fintrust-deployer

  namespace: default

roleRef:

  kind: Role

  name: fintrust-deployer-role

  apiGroup: rbac.authorization.k8s.io